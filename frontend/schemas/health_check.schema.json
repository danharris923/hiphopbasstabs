{
  "description": "Health check response model.",
  "properties": {
    "status": {
      "description": "Service status",
      "enum": [
        "ok",
        "error"
      ],
      "title": "Status",
      "type": "string"
    },
    "timestamp": {
      "description": "Check timestamp",
      "format": "date-time",
      "title": "Timestamp",
      "type": "string"
    },
    "version": {
      "default": "1.0.0",
      "description": "API version",
      "title": "Version",
      "type": "string"
    }
  },
  "required": [
    "status"
  ],
  "title": "HealthCheck",
  "type": "object",
  "$id": "https://basstabs.com/schemas/health_check"
}